# Testing Guide

Run backend route tests with `npm run test:server`.

Run frontend lint checks with `npm run test:client`.

Run ML engine tests with `npm run test:ml`.

Run all configured checks individually with `npm run test:server`, `npm run test:client`, and `npm run test:ml`.

If GNU Make is installed, `make test` runs the same three checks.
